import fs from "fs";

const transform = async () => {
    // Write your code here
    const writableStreamNew = 'This file should be read using Streams API write'
    let a
    a.transform(writableStreamNew)
};

await transform();
